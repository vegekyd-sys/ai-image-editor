import fs from 'node:fs';
import path from 'node:path';
import {execFileSync} from 'node:child_process';
const root=path.resolve('artifacts/core-prompt');
const json=p=>JSON.parse(fs.readFileSync(p,'utf8'));
const captures=fs.readdirSync(root,{withFileTypes:true}).filter(e=>e.isDirectory()).flatMap(e=>fs.readdirSync(path.join(root,e.name)).filter(n=>/^routing-.*\.json$/.test(n)).map(n=>path.join(root,e.name,n))).sort();
const all=captures.flatMap(p=>json(p).rows.map(r=>({...r,capture:path.relative(root,p)})));
const latencyFile=captures.find(p=>p.includes('/latency-isolated/'));if(!latencyFile)throw new Error('Missing isolated latency run');
const latency=json(latencyFile);
const median=a=>{a=a.filter(v=>v!=null).sort((a,b)=>a-b);return !a.length?null:a.length%2?a[(a.length-1)/2]:(a[a.length/2-1]+a[a.length/2])/2};
const timings=['all',...new Set(latency.rows.map(r=>r.case))].map(id=>({id,...Object.fromEntries(['baseline','candidate'].map(v=>{const rows=latency.rows.filter(r=>r.variant===v&&(id==='all'||r.case===id));return[v,{n:rows.length,firstTextMs:median(rows.map(r=>r.firstTextMs)),firstActionMs:median(rows.map(r=>r.firstActionMs)),totalMs:median(rows.map(r=>r.totalMs))}]}))}));
const latest=new Map();
for(const r of [...all].sort((a,b)=>Number(a.capture.match(/routing-(\d+)/)[1])-Number(b.capture.match(/routing-(\d+)/)[1])))latest.set(r.case+':'+r.variant,r);
const originalFailures=all.filter(r=>r.failures.length).map(r=>({case:r.case,variant:r.variant,capture:r.capture,failures:r.failures}));
const scoreLatest=r=>r.case==='video-frame-repair'?r.calls.some(c=>c.name==='preview_frame'&&c.input.media_index===1&&c.input.timestamp===3):r.passed;
const videoFiles=['media/video/video-direct-baseline-0.mp4','media/video/video-direct-candidate-0.mp4','code/composition-baseline.mp4','code/composition-candidate.mp4'];
const videoProbes=videoFiles.map(file=>{const p=path.join(root,file);const meta=JSON.parse(execFileSync('ffprobe',['-v','error','-show_streams','-show_format','-of','json',p],{encoding:'utf8'}));execFileSync('ffmpeg',['-v','error','-i',p,'-f','null','-']);const v=meta.streams.find(s=>s.codec_type==='video');return{file,duration:Number(meta.format.duration),width:v.width,height:v.height,codec:v.codec_name,audio:meta.streams.some(s=>s.codec_type==='audio'),fullyDecoded:true};});
const summary={status:'local candidate; full hosted quality non-regression not yet accepted',baseRevision:'3193cd36',host:latency.host,agentModel:latency.model,inventory:latency.inventory,modelRuns:all.length,distinctCases:new Set(all.map(r=>r.case)).size,latestRouting:{passed:[...latest.values()].filter(scoreLatest).length,total:latest.size,oracleCorrection:'Timestamp-based frame repair first uses preview_frame, per video-segment-edit/SKILL.md lines 69-89; raw old failures are retained.'},originalFailures,timings,videoProbes,mediaCaveats:['Initial local-color candidate changed bow-tie pattern; core fidelity rule added and retested.','One OpenRouter retest fell back to Qwen. Four Google-direct tests hit a location restriction and also fell back. Exclude those from same-model comparison.','Both Remotion first drafts had outer-JSX syntax errors. One controlled repair each passed validation and local rendering/props patch. This is not hosted recovery acceptance.','No general human-identity, complete multi-source production, or hosted CUI/CLI/Preview/Export acceptance claim.']};
fs.writeFileSync('docs/core-prompt-refactor/results.json',JSON.stringify(summary,null,2)+'\n');
const inv=summary.inventory;const a=inv.baseline.systemChars+inv.baseline.toolChars,b=inv.candidate.systemChars+inv.candidate.toolChars;
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const pairs=[['精确改色：第一次的反例','media/dog-local/media-dog-local-baseline-0.jpg','media/dog-local/media-dog-local-candidate-0.jpg','候选改变了领结纹样；这组判失败。'],['精确改色：补强后复测','media/dog-local-retest/media-dog-local-baseline-0.jpg','media/dog-local-retest/media-dog-local-candidate-0.jpg','两版保留黑色条纹。有限样本，不代表像素级保真保证。'],['专业增强','media/dog-enhance/media-dog-enhance-baseline-0.jpg','media/dog-enhance/media-dog-enhance-candidate-0.jpg','同一原图、同一图片模型；查看细节和主体保真。'],['整体风格化','media/dog-style/media-dog-style-baseline-0.jpg','media/dog-style/media-dog-style-candidate-0.jpg','旧创作模板逐字保留。'],['10秒原生视频','media/video/video-direct-baseline-0.mp4','media/video/video-direct-candidate-0.mp4','SeeDance Fast，同一请求，两版均完整解码。'],['5秒 Remotion 动画','code/composition-baseline.mp4','code/composition-candidate.mp4','两版原始首稿都需要一次语法修复；用户未指定画幅，两版自行选择不同画幅。这里只通过本地渲染。'],['可编辑 props 修改','code/composition-baseline-patched.png','code/composition-candidate-patched.png','只修改 title prop 后重新截图；其余源代码不变。']];
const render=(p,label)=>`<figure>${p.endsWith('.mp4')?`<video controls playsinline preload="metadata" src="${p}"></video>`:`<a href="${p}" target="_blank"><img loading="lazy" src="${p}" alt="${label}"></a>`}<figcaption>${label}</figcaption></figure>`;
const sections=pairs.map(([title,a,b,note],i)=>`<section><h2>${title}</h2><p>${note}</p><div class="pair">${i%2?render(b,'B')+render(a,'A'):render(a,'A')+render(b,'B')}</div><details><summary>显示版本</summary>A = baseline，B = candidate。左右交错，未使用模型自评分。</details></section>`).join('');
fs.writeFileSync(path.join(root,'review.html'),`<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Core Prompt · 重构验收</title><style>body{margin:0;background:#f5f4ef;color:#202822;font:16px/1.7 -apple-system,BlinkMacSystemFont,sans-serif}main{max-width:1160px;margin:60px auto;padding:0 24px}h1{font-size:42px;letter-spacing:-2px}h2{font-size:24px;margin:0}p{color:#576459}section{padding:28px;background:white;border:1px solid #e0e5dd;border-radius:18px;margin:24px 0}.pair{display:grid;grid-template-columns:1fr 1fr;gap:20px}figure{margin:0}img,video{width:100%;max-height:600px;object-fit:contain;background:#eef0eb;border-radius:10px}figcaption{text-align:center;color:#687567}details{margin-top:12px}table{width:100%;border-collapse:collapse}td,th{text-align:left;padding:10px;border-bottom:1px solid #e0e5dd}.tag{display:inline-block;background:#e3eade;color:#3c5740;padding:6px 16px;border-radius:30px}small{color:#667361}@media(max-width:700px){.pair{grid-template-columns:1fr}main{margin:30px auto}h1{font-size:32px}}</style><main><span class="tag">独立 worktree · 本地候选</span><h1>Core Prompt，减负与保真</h1><p>静态 system + tools：${a.toLocaleString()} → ${b.toLocaleString()} 字符（-${((1-b/a)*100).toFixed(1)}%）。54 段原 Core 规则可追溯，12 份关键创作文件逐字保留。</p><p>${summary.distinctCases} 个场景 / ${summary.modelRuns} 次真实 Agent 模型记录。首动作选路与真实媒体质量分开验收。未合并、未部署；不能将有限样本称为全场景效果不下降。</p><section><h2>普通场景速度 · 五轮交错 A/B</h2><p>Mac，Azure GPT-5.6 Terra。单位毫秒；动作耗时止于媒体调用，未包括生成耗时。缓存与网络波动仍然存在。</p><table><tr><th>场景</th><th>首字 A → B</th><th>首次动作 A → B</th></tr>${timings.map(r=>`<tr><td>${esc(r.id)}</td><td>${r.baseline.firstTextMs} → ${r.candidate.firstTextMs}</td><td>${r.baseline.firstActionMs??'—'} → ${r.candidate.firstActionMs??'—'}</td></tr>`).join('')}</table><p>enhance 路径在本次样本中变慢，未隐藏。更短的上下文不保证每个请求都更快。</p></section>${sections}<section><h2>验收边界</h2><ul>${summary.mediaCaveats.map(s=>`<li>${esc(s)}</li>`).join('')}</ul></section></main></html>`);
console.log(JSON.stringify({runs:summary.modelRuns,cases:summary.distinctCases,latest:summary.latestRouting,chars:{baseline:a,candidate:b}}));
